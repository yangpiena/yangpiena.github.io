#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../../../_swig-templates@2.0.3@swig-templates/bin/swig.js" "$@"
  ret=$?
else 
  node  "$basedir/../../../_swig-templates@2.0.3@swig-templates/bin/swig.js" "$@"
  ret=$?
fi
exit $ret
